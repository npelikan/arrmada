Thank you for installing {{ .Chart.Name }} v{{ .Chart.Version }}!

Services deployed:
{{- if .Values.sonarr.enabled }}
  • Sonarr (TV):   http://{{ include "arrmada.componentName" (dict "root" . "component" "sonarr") }}.{{ .Release.Namespace }}.svc:{{ .Values.sonarr.service.port }}
{{- end }}
{{- if .Values.radarr.enabled }}
  • Radarr (Movies): http://{{ include "arrmada.componentName" (dict "root" . "component" "radarr") }}.{{ .Release.Namespace }}.svc:{{ .Values.radarr.service.port }}
{{- end }}
{{- if .Values.prowlarr.enabled }}
  • Prowlarr (Indexers): http://{{ include "arrmada.componentName" (dict "root" . "component" "prowlarr") }}.{{ .Release.Namespace }}.svc:{{ .Values.prowlarr.service.port }}
{{- end }}

Check service status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=sonarr
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=radarr
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=prowlarr
{{- if .Values.configSync.enabled }}

Config sync job logs:
  kubectl logs -n {{ .Release.Namespace }} job/{{ include "arrmada.fullname" . }}-config-sync
{{- end }}
