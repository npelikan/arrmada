{{- if .Values.radarr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "arrmada.componentName" (dict "root" . "component" "radarr") }}
  labels:
    {{- include "arrmada.componentLabels" (dict "root" . "component" "radarr") | nindent 4 }}
spec:
  type: {{ .Values.radarr.service.type }}
  ports:
    - port: {{ .Values.radarr.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arrmada.componentSelectorLabels" (dict "root" . "component" "radarr") | nindent 4 }}
{{- end }}
