{{- if .Values.rtorrent.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "arrmada.componentName" (dict "root" . "component" "rtorrent") }}
  labels:
    {{- include "arrmada.componentLabels" (dict "root" . "component" "rtorrent") | nindent 4 }}
spec:
  type: {{ .Values.rtorrent.service.type }}
  ports:
    - port: {{ .Values.rtorrent.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arrmada.componentSelectorLabels" (dict "root" . "component" "rtorrent") | nindent 4 }}
{{- end }}
