suite: rtorrent ingress
templates:
  - rtorrent/ingress.yaml
values:
  - ../values-test.yaml
tests:
  - it: should produce no documents when ingress list is empty
    asserts:
      - hasDocuments:
          count: 0

  - it: should create one Ingress per enabled entry
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
                  pathType: Prefix
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Ingress

  - it: should use entry name as Ingress name suffix
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
    asserts:
      - equal:
          path: metadata.name
          value: RELEASE-NAME-arrmada-rtorrent-nginx

  - it: should skip disabled entries
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: false
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
    asserts:
      - hasDocuments:
          count: 0

  - it: should set ingressClassName when className is specified
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
    asserts:
      - equal:
          path: spec.ingressClassName
          value: nginx

  - it: should create two Ingress resources for two enabled entries
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
        - name: tailscale
          enabled: true
          className: tailscale
          hosts:
            - host: rtorrent
              paths:
                - path: /
    asserts:
      - hasDocuments:
          count: 2

  - it: should set TLS configuration when provided
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
          tls:
            - secretName: rtorrent-tls
              hosts:
                - rtorrent.example.com
    asserts:
      - isNotEmpty:
          path: spec.tls

  - it: should pass annotations through
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          annotations:
            cert-manager.io/cluster-issuer: letsencrypt-prod
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
    asserts:
      - equal:
          path: metadata.annotations["cert-manager.io/cluster-issuer"]
          value: letsencrypt-prod

  - it: should produce no Ingress when rtorrent is disabled
    set:
      rtorrent.enabled: false
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
    asserts:
      - hasDocuments:
          count: 0

  - it: should route to rtorrent service backend
    set:
      rtorrent.ingress:
        - name: nginx
          enabled: true
          className: nginx
          hosts:
            - host: rtorrent.example.com
              paths:
                - path: /
    asserts:
      - equal:
          path: spec.rules[0].http.paths[0].backend.service.name
          value: RELEASE-NAME-arrmada-rtorrent
      - equal:
          path: spec.rules[0].http.paths[0].backend.service.port.name
          value: flood
