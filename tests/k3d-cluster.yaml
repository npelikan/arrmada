apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: arrmada-test
servers: 1
agents: 0
options:
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters: [server:*]
  kubeconfig:
    updateDefaultKubeconfig: false   # Never touch ~/.kube/config
    switchCurrentContext: false
